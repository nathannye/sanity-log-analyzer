{
  "name": "sanity-log-parser",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "sanity-log-parser": "dist/analyze.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "analyze": "npm run build && node dist/analyze.js",
    "to-csv": "npm run build && node dist/to-csv.js",
    "test": "npm run build && node --test dist/aggregate.test.js dist/report.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  }
}
